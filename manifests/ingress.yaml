apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-ui-ingress
  labels:
    app.kubernetes.io/name: todo-ui-ingress
spec:
  rules:
  - host: ui.jhandu.shop
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: todo-ui
            port: 
              number: 80
